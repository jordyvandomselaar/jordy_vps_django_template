version: "3.9"

services:
  db:
    extends:
      file: ./docker/db.yml
      service: db
  web:
    extends:
      file: ./docker/web.yml
      service: web
    depends_on:
      - db
volumes:
  static-data:
  db-data: